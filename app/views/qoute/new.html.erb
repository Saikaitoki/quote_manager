<!-- app/views/quotes/new.html.erb -->
<div class="container mt-5">
  <h1 style="color: red;">DEBUG: NEW QUOTE PAGE</h1>

  <div class="card shadow-sm">
    <div class="card-header bg-primary text-white">
      見積書 新規作成
    </div>
    <div class="card-body">
      <%= render "form", quote: @quote %>
    </div>
  </div>
</div>
